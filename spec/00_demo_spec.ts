describe('writing a spec', () => {
    it('is easy when you know how!', () => {
        //Given
        const a = 10, b = 5;

        //When
        const answer = a + b;

        //Then
        expect(answer).toBe(15);
    });
});